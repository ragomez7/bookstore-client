import React  from 'react';

export const BooksContext = React.createContext(null);
export const ReadersContext = React.createContext(null);
export const AuthorsContext = React.createContext(null);
export const halle = "halle";